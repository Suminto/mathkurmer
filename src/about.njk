---
layout: layouts/base.njk
permalink: "/about/"
eleventyComputed:
  title: "{{ pagesContent.about.judul }}"
  description: "{{ pagesContent.about.eyebrow }}"
---
<div class="content-inner">
  <p class="eyebrow">{{ pagesContent.about.eyebrow }}</p>
  <h1 class="page-title">{{ pagesContent.about.judul }}</h1>
  <div class="hero-card">
    {{ pagesContent.about.isi | markdown | safe }}
    <p class="credit-line">{{ pagesContent.about.creditLine }}</p>
  </div>
</div>
